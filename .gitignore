node_modules/
.next/
out/
.DS_Store
.env*

.vercel
.env*

# extracted KeyShot source textures used only as conversion-script input (see scripts/convert-halogrip-model.mjs)
scripts/_model-source/

# local dev-server logs and TS incremental build cache — regenerated on every `npm run dev`/`tsc`
dev-server.log
dev-server-err.log
tsconfig.tsbuildinfo

# Local research and design-source material
references/
design-source/
*.zip
~$*

# Probe renders from scripts/convert-pdf-pages.mjs --probe (regenerated on demand)
scratch/

# geodata cache for scripts/build-seme-locator.mjs (re-fetched on demand, ~9MB)
scripts/.cache/
